name: Manual workflow

on: 
  workflow_dispatch:
    inputs:
      name:
        description: 'Name'
        type: string
        default: 'test'

jobs: 
  manual:
    runs-on: ubuntu-latest

    steps: 
      - name: Greeting
        run: echo "${{ inputs.name }}"
